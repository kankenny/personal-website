class LinkClass {
	linkText: string
	isSelected: boolean

	constructor(linkText: string, isSelected: boolean) {
		this.linkText = linkText
		this.isSelected = isSelected
	}
}

export default LinkClass
